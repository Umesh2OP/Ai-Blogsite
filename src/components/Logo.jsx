import React from 'react';
import './Logo.css'; // still uses the glowing CSS

const Logo = () => {
  return (
    <div className="text-lg sm:text-xl font-bold text-white neon-glow tracking-wide">
      BLOG-VERSE
    </div>
  );
};

export default Logo;
